export const all = [
  'q', 'institutionKey', 'city', 'country', 'code'
].sort();

const highlighted = [
  'q', 'institutionKey', 'city', 'country', 'code'
];

export default { filters: all, included: all, highlighted };