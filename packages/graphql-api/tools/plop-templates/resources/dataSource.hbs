// const { ApolloError } = require('apollo-server');
const { RESTDataSource } = require('apollo-datasource-rest');
const config = require('../../config');
const API_V1 = config.API_V1;

class {{pascalCase name}}API extends RESTDataSource {
  constructor() {
    super();
    this.baseURL = API_V1;
  }

  async search{{pascalCase name}}s({ query }) {
    return this.get('/{{camelCase name}}', query);
  }

  async get{{pascalCase name}}ByKey({ key }) {
    return this.get(`/{{camelCase name}}/${key}`);
  }

  /*
  get{{pascalCase name}}sByKeys({ {{camelCase name}}Keys }) {
    return Promise.all(
      {{camelCase name}}Keys.map(key => this.get{{pascalCase name}}ByKey({ key })),
    );
  }
  */
}

module.exports = {{pascalCase name}}API;