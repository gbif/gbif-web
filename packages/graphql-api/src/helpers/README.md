# A mixed bag of utility api wrappers

A place to locate complex logic related to 
* wrappers for partner APIs (e.g. wikidata wrapper)
* endpoints and formaters that isn't resources, but supplementary data (e.g. format a scientific name)