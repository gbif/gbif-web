import { ArticleContainer } from '@/routes/resource/key/components/articleContainer';
import { ArticleTextContainer } from '@/routes/resource/key/components/articleTextContainer';

export function DatasetKeyOccurrences() {
  return (
    <ArticleContainer className="g-bg-slate-100 g-pt-0">
      <ArticleTextContainer className="g-max-w-screen-xl">Occurrence search - will be added once we have the explorer ready</ArticleTextContainer>
    </ArticleContainer>
  );
}
